SELECT COD_AGENTE FROM VENDITE GROUP BY COD_AGENTE

SELECT NUM_ORDINE FROM VENDITE INNER JOIN COD_AGENTE FROM AGENTI ON VENDITE.COD_AGENTE = AGENTI.COD_AGENTE GROUP BY COD_AGENTE

SELECT COUNT (NUM_ORDINE) FROM VENDITE

SELECT COUNT (NUM_ORDINE) FROM VENDITE INNER JOIN AGENTI ON VENDITE.COD_AGENTE = AGENTI.COGNOME

SELECT * FROM VENDITE INNER JOIN AGENTI ON VENDITE.COD_AGENTE = AGENTI.COGNOME