SELECT COD_AGENTE FROM AGENTI WHERE SEDE = 'PADOVA'
SELECT  MAX(STIPENDIO), MIN(STIPENDIO), AVG(STIPENDIO) FROM AGENTI WHERE SEDE = 'PADOVA'
SELECT  NOME FROM AGENTI WHERE STIPENDIO >= 4000000 AND STIPENDIO <= 5000000
SELECT  NOME FROM AGENTI WHERE STIPENDIO BETWEEN 4000000 AND 5000000
SELECT  COGNOME FROM AGENTI WHERE SEDE IN ('MILANO', 'ROMA')
SELECT SOCIETA FROM CLIENTLA WHERE SOCIETA LIKE "L%"
SELECT SEDE FROM AGENTI WHERE SEDE LIKE "_MA"
SELECT COD_ARTIC FROM ARTICOLI WHERE NOT CESSATO
SELECT SOCIETA FROM CLIENTLA WHERE CITTA = 'ROMA' ORDER BY SOCIETA ASC
SELECT SOCIETA, COGNOME, NOME FROM CLIENTLA ORDER BY 2 ASC
SELECT COD_ARTIC, SUM(DISPONIBIL) FROM ARTICOLI GROUP BY COD_ARTIC ORDER BY 2
SELECT COUNT(*), SEDE  FROM AGENTI GROUP BY SEDE 
SELECT COUNT(*),SEDE FROM AGENTI GROUP BY SEDE HAVING COUNT(*)>3
SELECT DISTINCT COD_ARTIC FROM ARTICOLI
SELECT COD_ARTIC,SUM(DISPONIBIL) FROM ARTICOLI GROUP BY COD_ARTIC
SELECT COD_ARTIC,SUM(DISPONIBIL) FROM ARTICOLI GROUP BY COD_ARTIC HAVING SUM(DISPONIBIL)>10
SELECT COD_ARTIC,SUM(DISPONIBIL) FROM ARTICOLI GROUP BY COD_ARTIC HAVING SUM(DISPONIBIL)>10 ORDER BY COD_ARTIC
SELECT DESCRIZION,SUM(DISPONIBIL) FROM ARTICOLI WHERE PREZZO>500000 GROUP BY DESCRIZION,COD_ARTIC
SELECT DESCRIZION,SUM(DISPONIBIL) FROM ARTICOLI GROUP BY DESCRIZION  HAVING SUM(DISPONIBIL)>300
SELECT SEDE FROM AGENTI UNION SELECT MAGAZZINO FROM ARTICOLI
SELECT NUM_ORDINE, DAT_ORDINE,COGNOME FROM VENDITE,AGENTI WHERE VENDITE.COD_AGENTE=AGENTI.COD_AGENTE
SELECT NUM_ORDINE, DAT_ORDINE,COGNOME FROM VENDITE,AGENTI WHERE VENDITE.COD_AGENTE AND AGENTI.COGNOME="Zonin"
SELECT NUM_ORDINE, DAT_ORDINE,COGNOME FROM AGENTI INNER JOIN VENDITE ON AGENTI.COD_AGENTE=VENDITE.COD_AGENTE
SELECT NUM_ORDINE, DAT_ORDINE,COGNOME FROM AGENTI INNER JOIN VENDITE ON AGENTI.COD_AGENTE=VENDITE.COD_AGENTE WHERE AGENTI.COGNOME="Rolandi"
SELECT COUNT(*),COD_AGENTE FROM VENDITE GROUP BY COD_AGENTE HAVING COD_AGENTE="000015"
SELECT COUNT(*) AS N_VENDITE,COD_AGENTE FROM VENDITE GROUP BY COD_AGENTE HAVING COD_AGENTE="000015"

SELECT ISP.COGNOME, "COORDINA", VEN.COGNOME FROM AGENTI VEN, AGENTI ISP WHERE VEN.COD_AGENTE=ISP.ISPETTORE

SELECT  ISP.COGNOME, "COORDINA", VEN.COGNOME  FROM AGENTI VEN INNER JOIN AGENTI ISP  ON VEN.COD_AGENTE=ISP.ISPETTORE WHERE VEN.COD_AGENTE=ISP.ISPETTORE ORDER BY ISP.COGNOME

SELECT ISP.SEDE,ISP.COGNOME, VEN.COGNOME,VEN.SEDE FROM AGENTI VEN INNER JOIN AGENTI ISP ON VEN.SEDE=ISP.SEDE WHERE VEN.COD_AGENTE>ISP.COD_AGENTE 

SELECT COUNT(*) FROM AGENTI VEN INNER JOIN AGENTI ISP ON VEN.SEDE=ISP.SEDE WHERE VEN.COD_AGENTE>ISP.COD_AGENTE 


SELECT NUM_ORDINE FROM VENDITE WHERE COD_CLIENT=(SELECT COD_CLIENT FROM CLIENTLA WHERE SOCIETA='Errebi') 

SELECT NUM_ORDINE FROM VENDITE INNER JOIN CLIENTLA ON VENDITE.COD_CLIENT= CLIENTLA.COD_CLIENT WHERE SOCIETA='Errebi'

quali sono gli agenti che hanno uno stipendio che è maggiore della media degli stipendi

SELECT COGNOME,STIPENDIO FROM AGENTI WHERE STIPENDIO> (SELECT AVG(STIPENDIO) FROM AGENTI)

quali sono gli articoli che hanno il prezzo uguale all'articolo con prezzo maggiore

SELECT DISTINCT DESCRIZION FROM ARTICOLI WHERE PREZZO=(SELECT MAX(PREZZO) FROM ARTICOLI)

visualizzare ordini che hanno come codice cliente errebi o come codice cliente zonin senza usare codice utente nè codice cliente

SELECT NUM_ORDINE FROM VENDITE WHERE COD_CLIENT=(SELECT COD_CLIENT FROM CLIENTLA WHERE SOCIETA='Errebi') OR COD_AGENTE=(SELECT COD_AGENTE FROM AGENTI WHERE COGNOME='Zonin')

query niterna ritorna una colonna di valori,
estrarre informazioni sugli ordini dei clienti della provincia di Milano
num ordine, codice cliente, data relativa ai clienti della provincia di Milano

SELECT NUM_ORDINE, COD_CLIENT,DAT_ORDINE FROM VENDITE WHERE COD_CLIENT IN (SELECT COD_CLIENT FROM CLIENTLA WHERE CAP LIKE '20%')

operatori any/all
any:confronto effettuato con any risulta vero se vero per almeno uno dei valori
 presenti all'interno di una colonna di valori
se la condizione è soddisfatta per almeno un valore della query più interna 

all: il confronto risulta vero se è vero per tutti i valori restituiti dalla colonna 

visualizzare codice articolo dove il prezzo è maggiore almeno del prezzo di un certo articolo

SELECT COD_ARTIC,PREZZO FROM ARTICOLI


visualizzare informazioni venditori la cui provvigione è inferiore a quella di almeno uno dei venditori della sede di Milano

SELECT COD_AGENTE FROM AGENTI WHERE PROVVIGION<ANY (SELECT PROVVIGION FROM AGENTI WHERE SEDE='MILANO')


visualizzare informazioni relative ai venditori la cui provvigione è pià alta di qualsiasi provvigione di un agente di Milano
agenti la cui provvigione> provvigione di quelli di Milano

(SELECT PROVVIGION FROM AGENTI WHERE SEDE='MILANO)
